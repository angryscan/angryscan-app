include required("conveyor-common.conf")

app {
  machines = [
    linux.amd64.glibc,
    mac.amd64,
    mac.aarch64
  ]

  build.gradle {
    env = {

    }
  }
}

project.gradleArgs = ["-PtargetOs=unix"]